## 2024-05-18 - explicit required indicators and loading states
**Learning:** Multi-step forms greatly benefit from explicit required indicators on inputs (e.g., `aria-required`) and visual loading states on the final submission step to prevent user confusion and double-submits.
**Action:** Always add explicit `required` indicators and loading states (`isSubmitting`) when building multi-step forms in this application.
